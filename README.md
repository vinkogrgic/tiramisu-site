# Tiramisu site

Static sales page. Deploy: push this folder to a GitHub repo, enable Pages (Settings -> Pages -> Deploy from branch `main`, root). Edit `index.html` directly (press `.` in the repo for github.dev). Page is set to noindex.
